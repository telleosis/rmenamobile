
    import 'package:flutter/material.dart';
    class TuitionPage extends StatelessWidget {
        const TuitionPage({Key? key}) : super(key: key);
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: Text('Tuition Page'),
            ),
            );
            }
        }
    